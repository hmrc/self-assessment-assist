summary: Retrieve a UK Property Business Annual Submission
description: |
  This endpoint allows a developer to aaaaaaaaa

tags:
    - HMRC Self Assessment Assist Report

parameters:
    - $ref: './pathParameters/calculationId.yaml'
    - $ref: '../pathParameters.yaml#/components/parameters/businessId'

security:
  - User-Restricted:
      - read:self-assessment

responses:
  "200":
    description: Success